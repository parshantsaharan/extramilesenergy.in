// config.js - GitHub Configuration
const GITHUB_CONFIG = {
    USERNAME: 'parshantsahara',
    REPO: 'extramilesenergy.in',
    BRANCH: 'main',
    PRODUCTS_PATH: 'content/products',
    UPLOADS_PATH: 'uploads',
    
    // आपको यह Token बनाना होगा GitHub पर
    // GitHub → Settings → Developer Settings → Personal Access Tokens → Generate new token
    // Permissions: repo (full control)
    TOKEN: 'YOUR_GITHUB_PERSONAL_ACCESS_TOKEN' // अपना token यहाँ डालें
};
